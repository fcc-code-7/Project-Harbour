@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page with Navbar</title>
    <link href="~/bootstrap-5.3.3-dist/css/bootstrap.css" rel="stylesheet" />
    <link href="~/bootstrap-5.3.3-dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="~/bootstrap-5.3.3-dist/css/bootstrap.rtl.css" rel="stylesheet" />
    <link href="~/bootstrap-5.3.3-dist/css/bootstrap.rtl.min.css" rel="stylesheet" />
@*     <a href="~/bootstrap-5.3.3-dist/css/bootstrap.css.map"></a>
    <a href="~/bootstrap-5.3.3-dist/css/bootstrap.min.css.map"></a>
    <a href="~/bootstrap-5.3.3-dist/css/bootstrap.rtl.css.map"></a>
    <a href="~/bootstrap-5.3.3-dist/css/bootstrap.rtl.min.css.map"></a> *@
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        /* For Chrome, Safari, Edge, and Opera */
        ::-webkit-scrollbar {
            width: 6px; /* Set the width of the scrollbar */
            height: 6px; /* Set the height of the scrollbar (for horizontal scrollbars) */
        }

        ::-webkit-scrollbar-track {
            background: transparent; /* Background of the scrollbar track */
        }

        ::-webkit-scrollbar-thumb {
            background-color: rgba(0, 0, 0, 0.2); /* Scrollbar color with transparency */
            border-radius: 10px; /* Rounded corners for the scrollbar */
        }

            ::-webkit-scrollbar-thumb:hover {
                background-color: rgba(0, 0, 0, 0.5); /* Darker on hover */
            }

        /* Custom styles */
        body,
        html {
            height: 100%;
            margin: 0;
        }

        .background-image {
            background-image: url('../images/homePage_bg.jpg');
            height: 90.5vh;
            background-size: cover;
            background-position: center;
        }

        .backdrop {
            display: none;
        }

        .cursorChange {
            cursor: pointer;
        }
        .backdrop.open {
            display: block;
        }

        .cursor {
            display: inline-block;
            animation: blink 1s steps(2, start) infinite;
        }

        @@keyframes blink {
            to {
                visibility: hidden;
            }
        }
        @@media screen and (max-width: 480px) {
            .mobileScreen{
                text-align: center;
            }
        }

        .logoSetting {
            width: 70px;
            height: 40px;
        }
    </style>
</head>

<body>
    <!-- Navbar Section -->

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#"><img src="~/Logo/fyp_logo-removebg-preview.png" class="logoSetting" /></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 mx-auto gap-3 mobileScreen">
                    <li class="nav-item">
                        <a class="cursorChange nav-link text-primary" aria-current="page" id="homeLink">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="cursorChange nav-link text-primary" aria-current="page" id="objectivesLink">Objectives</a>
                    </li>
                    <li class="nav-item">
                        <a class="cursorChange nav-link text-primary" aria-current="page" id="criteriaNPoliciesLink">Criteria & Policies</a>
                    </li>
                    <li class="nav-item">
                        <a class="cursorChange nav-link text-primary" aria-current="page" id="fypCommitteeLink">Fyp Committee</a>
                    </li>
                    <li class="nav-item">
                        <a class="cursorChange nav-link text-primary" aria-current="page" id="notificationsLink">Notifications</a>
                    </li>
                    <li class="nav-item">
                        <a class="cursorChange nav-link text-primary" aria-current="page" id="recentProjectsLink">Recent Projects</a>
                    </li>
                </ul>
                <!-- Buttons -->
                <div class="mobileScreen">
                    <button class="btn btn-primary text-white px-3 py-1 rounded" href="/account/login" id="login-btn">Login</button>
                </div>
            </div>
        </div>
    </nav>
    <!-- Home Page Section -->
    <section class="flex justify-center items-center h-screen background-image">
        <div class="bg-white mx-6 my-4">
            <h1 class="sm:text-2xl lg:text-5xl font-bold text-blue-700 mx-6 my-4" id="typewriter-text">
                <!-- Typewriter text will be inserted here -->
            </h1>
        </div>

        <!-- Optional: Outlet placeholder for nested components -->
        <div id="outlet">
            <!-- Nested content can be dynamically inserted here -->
        </div>
    </section>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/app-assets/js/core/libraries/jquery.min.js"></script>
    <script src="~/bootstrap-5.3.3-dist/js/bootstrap.bundle.js"></script>
    <script src="~/bootstrap-5.3.3-dist/js/bootstrap.bundle.min.js"></script>

    <script src="~/bootstrap-5.3.3-dist/js/bootstrap.js"></script>
    <script src="~/bootstrap-5.3.3-dist/js/bootstrap.min.js"></script>
    <script>
        $(document).ready(function () {
            // Typewriter effect script
            const $typewriterElement = $('#typewriter-text');
            const words = ['Welcome To Bukc Official Fyp Site'];
            let currentWordIndex = 0;
            let currentCharIndex = 0;
            let isDeleting = false;
            const typeSpeed = 120;
            const deleteSpeed = 80;

            function typewriterEffect() {
                const currentWord = words[currentWordIndex];

                if (!isDeleting && currentCharIndex < currentWord.length) {
                    // Typing the current character
                    $typewriterElement.html(currentWord.slice(0, currentCharIndex + 1) + '<span class="cursor">|</span>');
                    currentCharIndex++;
                    setTimeout(typewriterEffect, typeSpeed);
                } else if (isDeleting && currentCharIndex > 0) {
                    // Deleting the current character
                    $typewriterElement.html(currentWord.slice(0, currentCharIndex - 1) + '<span class="cursor">|</span>');
                    currentCharIndex--;
                    setTimeout(typewriterEffect, deleteSpeed);
                } else if (!isDeleting && currentCharIndex === currentWord.length) {
                    // Pause before starting to delete
                    isDeleting = true;
                    setTimeout(typewriterEffect, 1000);
                } else if (isDeleting && currentCharIndex === 0) {
                    // Move to the next word and start typing again
                    isDeleting = false;
                    currentWordIndex = (currentWordIndex + 1) % words.length;
                    setTimeout(typewriterEffect, typeSpeed);
                }
            }

            // Start the typewriter effect
            typewriterEffect();

            // Navbar script for toggling sidebar
            const $menuIcon = $('#menu-icon');
            const $closeIcon = $('#close-icon');
            const $sidebar = $('#sidebar');

            // Toggle sidebar visibility
            $menuIcon.on('click', function () {
                $sidebar.addClass('open');
            });

            $closeIcon.on('click', function () {
                $sidebar.removeClass('open');
            });

            // Navigation links
            $('.nav-link').on('click', function () {
                if ($(window).width() < 1024) { // Only close sidebar on small screens
                    $sidebar.removeClass('open');
                }
            });

            // Navigate on button click
            $('#login-btn').on('click', function () {
                window.location.href = '/account/login';
            });



            const pageLinks = {
                homeLink: '/', // Index route (no need for "/home")
                objectivesLink: '/objectives',
                criteriaNPoliciesLink: '/criteriaNPolicies',
                fypCommitteeLink: '/fypCommittee',
                notificationsLink: '/notifications',
                recentProjectsLink: '/recentProjects'
            };


            // Add click event to each nav item
            $('.nav-link').on('click', function () {
                    const linkId = $(this).attr('id'); // Get the ID of the clicked link
            const targetUrl = pageLinks[linkId]; // Find the corresponding URL
            if (targetUrl) {
                window.location.href = targetUrl; // Redirect to the target page
                    }
                });
            });


    </script>

@*     <a href="~/lib/jquery/dist/jquery.min.map">~/lib/jquery/dist/jquery.min.map</a>
 *@   
@*     <a href="~/bootstrap-5.3.3-dist/js/bootstrap.bundle.js.map">~/bootstrap-5.3.3-dist/js/bootstrap.bundle.js.map</a>
    <a href="~/bootstrap-5.3.3-dist/js/bootstrap.bundle.min.js.map">~/bootstrap-5.3.3-dist/js/bootstrap.bundle.min.js.map</a>
    <a href="~/bootstrap-5.3.3-dist/js/bootstrap.esm.js.map">~/bootstrap-5.3.3-dist/js/bootstrap.esm.js.map</a>
    <a href="~/bootstrap-5.3.3-dist/js/bootstrap.esm.min.js.map">~/bootstrap-5.3.3-dist/js/bootstrap.esm.min.js.map</a>
    <a href="~/bootstrap-5.3.3-dist/js/bootstrap.js.map">~/bootstrap-5.3.3-dist/js/bootstrap.js.map</a>
    <a href="~/bootstrap-5.3.3-dist/js/bootstrap.min.js.map">~/bootstrap-5.3.3-dist/js/bootstrap.min.js.map</a> *@
 
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
</body>

</html>
